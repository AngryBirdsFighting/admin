import Config from "../config"
class Fetch{
    constructor(){
        this.baseUrl = Config.baseUrl;
        this.fetchOptions = {
            method: 'GET',
            mode: "cors",
            headers: Header,
            credentials: 'include',
          };
    }
    request(option){
        if(option)
        
       
        fetch("www.baidu.com",{} )
    }
}